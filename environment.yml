name: graduation-project
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -e .
      - numpy>=2,<3
      - openai>=1.108,<2
      - opencv-python-headless>=4.10,<5
      - pillow>=11,<12
      - pyarrow>=23,<24
      - pytest>=9,<10
      - ruff>=0.15,<0.16
